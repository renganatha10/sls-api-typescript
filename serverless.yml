service: sls-api-typescript

frameworkVersion: ">=1.1.0 <2.0.0"

provider:
  name: aws
  runtime: nodejs12.x
  region: ap-south-1

functions:
  hello:
    handler: lib/handler.hello
    events:
      - http:
          path: hello
          method: get
